#TODO
Sachetto:

Create a execution database in the test suite to track for performance regressions:
http://www.corpit.ru/mjt/tinycdb.html

#BUGS:

The logger symbols are only exported to an executable if an static library linked to a shared library uses then. For now this is ok. But I think it will
be a pain in future releases.

#Generate computer HW Hash (very simple)
inxi -CNM |grep -e Topology  -e Device-1 | sha256sum

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

Berg

domain:
	- In function initialize_grid_with_plain_fibrotic_mesh_using_file() allow that the "fibrotic_positions" file come from the user input

linear_system:
	- Allow the Purkinje linear systemn to be solved by the GPU
	- Implement the GMRES linear system solver (see Bernardo's book as reference ...) 

DDM:
	- Revise the equations again with Pedro

save_mesh:
	- Refactor the code for the Purkinje-Coupling LAT and APD map write.

purkinje:
	- Implement a Purkinje restore state function

example_configs:
- Fix Elnaz examples
- Fix Pedro examples

scripts:
- Organize all the scripts
- Include the ParserVTUtoALG
- Add a conduction velocity calculator for Purkinje fibers




	

  
