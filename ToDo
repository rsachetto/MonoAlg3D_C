#TODO
Sachetto:

Create a execution database in the test suite to track for performance regressions:
http://www.corpit.ru/mjt/tinycdb.html

#BUGS:

The logger symbols are only exported to an executable if an static library linked to a shared library uses then. For now this is ok. But I think it will
be a pain in future releases.

#Generate computer HW Hash (very simple)
inxi -CNM |grep -e Topology  -e Device-1 | sha256sum

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

Berg

grid_purkinje:
	- Check the Purkinje module
	- Allocate the Purkinje structures only when necessary
config:
	- Add the option to save the LAT and the APD map in VTU/VTP format (use Sachetto structure that stores all the activation times) --> Calculate the mean APD

domain:
	- In function initialize_grid_with_plain_fibrotic_mesh_using_file() allow that the "fibrotic_positions" file come from the user input

linear_system:
	- Implement the GMRES linear system solver (see Bernardo's book as reference ...) 

DDM:
	- Fix the DDM assembly_matrix library
	- Revise all the examples to see if is still working

purkinje:
	- Update the Purkinje code.
	- Implement the Purkinje coupling using a sphere area
	- Use the new vector structure
	- Implement a Purkinje restore state function

save_mesh:
	- Update Purkinje/Coupling save mesh function

vtk_utils:
	- Update the Purkinje Polydata VTK write

example_configs:
- Fix the Purkinje examples
- Fix the custom mesh examples
- Fix DDM examples
- Fix Mixed Models examples
- Fix Elnaz examples
- Fix Pedro examples
- Check why the "gold_simulation_no_adapt_cg_gpu" is giving the following error at the end of the simulation:
 	malloc_consolidate(): invalid chunk size
- Check why the "gold_simulation_no_adapt" is giving the following error at the end of the simulation:
	free(): double free detected in tcache 2
- Ask Sachetto about the "libten_tusscher_3_sast1.so" for the "plain_mesh_tentusscher_adapt" simulation.
- Simulation "plain_mesh_ToRORd_fkatp_endo" is giving the following error:
	[ERR] Solver stoped due to NaN on time 159.500000. This is probably a problem with the cellular model solver.
- Simulation "plain_mesh_with_fibrosis_and_border_zone_inside_circle" is giving Segmentation fault.

scripts:
- Organize all the scripts
- Include the ParserVTUtoALG
- Add a conduction velocity calculator for Purkinje fibers




	

  
