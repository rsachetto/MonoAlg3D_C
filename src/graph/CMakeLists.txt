set(SOURCE_FILES_GRAPH graph.h graph.c)

ADD_LIBRARY(graph STATIC ${SOURCE_FILES_GRAPH})

TARGET_LINK_LIBRARIES(graph m)

if(NOT MSVC)
    TARGET_LINK_LIBRARIES(graph m)
    set_property(TARGET graph PROPERTY POSITION_INDEPENDENT_CODE ON)
endif()