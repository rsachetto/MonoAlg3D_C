
if [ -n "$CUDA_FOUND" ]; then
    TEST_OPT_DEPS=gpu_utils
fi

TESTS_STATIC_DEPS="solvers ini_parser string config ${TEST_OPT_DEPS} utils config_helpers vtk_utils yxml alg graph"
TESTS_DYNAMIC_DEPS="$CUDA_LIBRARIES $CRITERION_LIBRARIES z dl m"

COMPILE_EXECUTABLE "TestSuite" "tests.c" "" "$TESTS_STATIC_DEPS" "$TESTS_DYNAMIC_DEPS" "$CUDA_LIBRARY_PATH $CRITERION_LIBRARY_PATH" "-DCOMPILE_ZLIB $CRITERION_INCLUDE_PATH"
